<%=form_for @restaurant do |form| %>

	<%= form.label :name %>
	<%= form.text_field :name %>
<br>
	<%= form.label :description %>
	<%= form.text_area :description %>

<br>

<%= form.submit %>

<% end %>